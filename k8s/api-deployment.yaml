apiVersion: apps/v1
kind: Deployment
metadata:
  name: leiaqa-api
spec:
  replicas: 2
  selector:
    matchLabels:
      app: leiaqa-api
  template:
    metadata:
      labels:
        app: leiaqa-api
    spec:
      containers:
      - name: api
        image: eslamelshamy/leiaqa-api:v6
        ports:
        - containerPort: 7049
        env:
        - name: PORT
          value: "7049"
        - name: NODE_ENV
          value: "development"
        - name: DB_HOST
          value: "postgres-db-svc"
        - name: DB_PORT
          value: "5432"
        - name: DB_USER
          value: "leiaqa_user"
        - name: DB_NAME
          value: "leiaqa"
        - name: REDIS_HOST
          value: "redis-db-svc"
        - name: REDIS_PORT
          value: "6379"

        # SECRETS (Pulled from Kubernetes, not from GitHub)
        - name: DB_PASS
          valueFrom:
            secretKeyRef:
              name: api-secrets
              key: DB_PASS
        - name: JWT_SECRET
          valueFrom:
            secretKeyRef:
              name: api-secrets
              key: JWT_SECRET
        - name: FIREBASE_PRIVATE_KEY
          valueFrom:
            secretKeyRef:
              name: api-secrets
              key: FIREBASE_PRIVATE_KEY
        - name: AWS_ACCESS_KEY_ID
          valueFrom:
            secretKeyRef:
              name: api-secrets
              key: AWS_ACCESS_KEY_ID
        - name: AWS_SECRET_ACCESS_KEY
          valueFrom:
            secretKeyRef:
              name: api-secrets
              key: AWS_SECRET_ACCESS_KEY
        - name: SMTP_PASS
          valueFrom:
            secretKeyRef:
              name: api-secrets
              key: SMTP_PASS
